# Optional. Leave unset for local dev and Vercel — the base URL is detected from
# the incoming request headers. Set it only if you need to pin an absolute origin.
# NEXT_PUBLIC_SITE_URL=https://quickcart.vercel.app
